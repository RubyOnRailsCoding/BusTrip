<%= form_for(trip) do |f| %>
  <% if trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :depart_city %>
    <%= f.text_field :depart_city %>
  </div>

  <div class="field datetimepicker">
    <%= f.label :depart_time %>
    <%= f.datetime_select :depart_time, class: 'datetimepicker' %>
  </div>

  <div class="field">
    <%= f.label :arrival_city %>
    <%= f.text_field :arrival_city %>
  </div>

  <div class="field">
    <%= f.label :arrival_time %>
    <%= f.datetime_select :arrival_time %>
  </div>

  <div class="field">
    <%= f.label :free_seat %>
    <%= f.number_field :free_seat %>
  </div>

  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
